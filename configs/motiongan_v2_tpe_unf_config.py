{
    # Model version to train
    'model_version': 'v2',
    # Time preserving embedding (NOT COMPATIBLE with latent factor model)
    'time_pres_emb': True,
    # Joint unfolding
    'unfold': True,
}